# Model release boundary

The historical `bronze_ranker.pkl` was trained from competition replay/game
materials. It is therefore **not** included in this public release. Its
filename, digest, tree counts, and historical role are recorded in
[`../docs/provenance.md`](../docs/provenance.md), but the bytes are intentionally
excluded. This directory is not a model download location.
