# Submission release boundary

The exact competition `main.py` and `deck.csv` are intentionally not included.
The deck contains game/card identifiers and the entrypoint depends on the
restricted official runtime. This repository is a source-only methodology
release, not a valid post-competition submission archive. A caller with a
separate authorization must supply any competition-specific adapter and deck
locally; no such files should be committed here.
